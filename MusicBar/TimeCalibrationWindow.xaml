<Window x:Class="MusicBar.TimeCalibrationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="校准歌词同步" Width="340" Height="190"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ShowInTaskbar="False" Topmost="True"
        Background="{DynamicResource PanelBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="输入播放器当前显示的播放时间" FontSize="15" FontWeight="SemiBold"
                   Foreground="{DynamicResource TextPrimary}" />
        <TextBlock Grid.Row="1" Text="格式：分:秒，例如 1:23" Margin="0,5,0,10"
                   Foreground="{DynamicResource TextSecondary}" />
        <TextBox x:Name="PositionTextBox" Grid.Row="2" Height="34" FontSize="15"
                 Padding="8,4" VerticalContentAlignment="Center" />
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="取消" Width="72" Height="30" Margin="0,0,8,0" IsCancel="True" />
            <Button Content="校准" Width="72" Height="30" IsDefault="True" Click="OnConfirmClick" />
        </StackPanel>
    </Grid>
</Window>
